#include "maskwalklinefinder.ih"

void MaskWalkLineFinder::initialise(minIni const& ini)
{
  // TODO populate stuff from ini file
  // TODO work out how to refresh the control values (cached in constructor) at this point
  //        - probably give the factory methods lamdas to obtain the current value
}